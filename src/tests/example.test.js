import React from 'react';
import { should, expect } from 'chai';
import { shallow, mount } from 'enzyme';
// import Click from './index';

should();

describe('<MonComposant />', () => {
  describe('TEST LANDING PAGE IN TESTS FOLDER', () => {
    it('should pass', () => {
      expect(true).to.be.false;
    });
  });
});
